# Netlify Secondary Fallback Check Workflow
name: Deploy to Netlify (Fallback)

on:
  workflow_dispatch:

jobs:
  check-fallback:
    runs-on: ubuntu-latest
    name: Secondary Host Fallback Status
    steps:
      - name: Primary Host Status
        run: |
          echo "✅ Primary production hosting active on Vercel (https://bethelmindanalytics.com)."
          echo "Netlify manual fallback pipeline configured."
