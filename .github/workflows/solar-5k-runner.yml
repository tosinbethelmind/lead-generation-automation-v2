name: ApexReach Nigeria Solar Pipeline Runner — 100% Real Leads

on:
  schedule:
    # Run every 20 minutes for continuous solar lead generation
    - cron: '*/20 * * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution Mode (live-solar | dry-run)'
        required: false
        default: 'live-solar'
      count:
        description: 'Target Lead Count'
        required: false
        default: '2500'
  repository_dispatch:
    types: [run-solar-5k]

jobs:
  run-solar-5k-pipeline:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm install --no-audit --no-fund

      - name: Install Playwright Chromium & System Dependencies
        run: npx playwright install chromium --with-deps || true

      - name: Execute Nigeria Real Solar Pipeline Scraper
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || secrets.SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN6eXV0ZXJuY2F3Znh3emh2d2NmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MjM5ODIwOSwiZXhwIjoyMDk3OTc0MjA5fQ._SzfC4NE4KCwWkK_GFQAyQjgkFrQLhbpz1w9R3FIUBY' }}
          BROWSERLESS_API_KEY: ${{ secrets.BROWSERLESS_API_KEY }}
          ACTIVE_BROWSER_PROVIDER: browserless
        run: |
          echo "🚀 Executing Nigeria Real Solar Pipeline Runner (100% Live Leads)..."
          MODE="${{ github.event.inputs.mode || github.event.client_payload.mode || 'live-solar' }}"
          COUNT="${{ github.event.inputs.count || github.event.client_payload.count || '2500' }}"
          JOB_ID="${{ github.event.client_payload.job_id || '' }}"

          # Quality Gate: synthetic mode is permanently disabled
          if [ "$MODE" = "synthetic" ]; then
            echo "⚠️  [Quality Gate] Synthetic mode requested — overriding to live-solar extraction."
            MODE="live-solar"
          fi

          ARGS="--count $COUNT"
          if [ -n "$JOB_ID" ]; then
            ARGS="$ARGS --run-id $JOB_ID"
          fi

          if [ "$MODE" = "dry-run" ]; then
            node scripts/nigeria_solar_5k_scraper.js --dry-run $ARGS || true
          else
            node scripts/nigeria_solar_5k_scraper.js $ARGS || true
          fi

      - name: Run Async Solar Harvester (Secondary Pass)
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || secrets.SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN6eXV0ZXJuY2F3Znh3emh2d2NmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MjM5ODIwOSwiZXhwIjoyMDk3OTc0MjA5fQ._SzfC4NE4KCwWkK_GFQAyQjgkFrQLhbpz1w9R3FIUBY' }}
        run: |
          echo "⚡ Running secondary solar harvester pass for maximum real leads..."
          node scripts/async_solar_5k_scraper.js || true

