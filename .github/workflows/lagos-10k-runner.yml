name: 10K Lagos B2B Engine — Distributed Matrix Runner

on:
  schedule:
    # Run every 20 minutes automatically across parallel zones
    - cron: '*/20 * * * *'
  workflow_dispatch:
    inputs:
      mode:
        description: 'Execution Mode (live | dry-run)'
        required: false
        default: 'live'
      count:
        description: 'Target Lead Count per zone'
        required: false
        default: '2000'
  repository_dispatch:
    types: [run-lagos-10k]

jobs:
  run-lagos-10k-matrix:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        zone: [ikeja_alausa, lekki_vi, yaba_surulere, ikorodu_oshodi, alimosho_apapa]

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm install --no-audit --no-fund

      - name: Install Playwright Chromium & System Dependencies
        run: npx playwright install chromium --with-deps || true

      - name: Execute Parallel Matrix Zone Harvester
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || secrets.SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN6eXV0ZXJuY2F3Znh3emh2d2NmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MjM5ODIwOSwiZXhwIjoyMDk3OTc0MjA5fQ._SzfC4NE4KCwWkK_GFQAyQjgkFrQLhbpz1w9R3FIUBY' }}
        run: |
          echo "🚀 Executing 10K Lagos B2B Matrix Node: ${{ matrix.zone }}..."
          MODE="${{ github.event.inputs.mode || github.event.client_payload.mode || 'live' }}"
          COUNT="${{ github.event.inputs.count || github.event.client_payload.count || '2000' }}"
          JOB_ID="${{ github.event.client_payload.job_id || '' }}"

          ARGS="--zone ${{ matrix.zone }} --count $COUNT"
          if [ -n "$JOB_ID" ]; then
            ARGS="$ARGS --run-id $JOB_ID"
          fi

          if [ "$MODE" = "dry-run" ]; then
            node scripts/async_lagos_10k_scraper.js --dry-run $ARGS || true
          else
            node scripts/async_lagos_10k_scraper.js $ARGS || true
          fi

      - name: Run Master Lagos Harvester (Zone Aggregation)
        if: matrix.zone == 'ikeja_alausa'
        env:
          NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL || secrets.SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_URL: ${{ secrets.SUPABASE_URL || secrets.NEXT_PUBLIC_SUPABASE_URL || 'https://szyuterncawfxwzhvwcf.supabase.co' }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY || secrets.SUPABASE_KEY || 'eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6InN6eXV0ZXJuY2F3Znh3emh2d2NmIiwicm9sZSI6InNlcnZpY2Vfcm9sZSIsImlhdCI6MTc4MjM5ODIwOSwiZXhwIjoyMDk3OTc0MjA5fQ._SzfC4NE4KCwWkK_GFQAyQjgkFrQLhbpz1w9R3FIUBY' }}
        run: |
          echo "📊 Running master Lagos harvester to aggregate OSM + Jiji leads..."
          COUNT="${{ github.event.inputs.count || github.event.client_payload.count || '10000' }}"
          JOB_ID="${{ github.event.client_payload.job_id || '' }}"
          ARGS="--count $COUNT"
          if [ -n "$JOB_ID" ]; then
            ARGS="$ARGS --run-id $JOB_ID"
          fi
          node scripts/lagos_10k_master_harvester.js $ARGS || true

